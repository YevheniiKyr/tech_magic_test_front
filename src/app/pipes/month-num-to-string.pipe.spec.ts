import { MonthNumToStringPipe } from './month-num-to-string.pipe';

describe('MonthNumToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthNumToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
